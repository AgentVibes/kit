---
"@agentvibes/kit": minor
---

Add `Mutation<E>` (keyed write state, optimistic edits with rollback, in-flight guard), `takeOut`/`putBack` optimistic list helpers, and `PagedQuery<T, C, E>` (cursor pagination with `loadMore`, `hasMore`, and an atomic `revalidate` that re-walks loaded pages).
